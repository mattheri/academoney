import { LoginForm } from "@/auth";

const LoginPage = () => {
  return (
    <section className="min-h-screen bg-gray-50 py-6 flex flex-col justify-center sm:py-12">
      <LoginForm />
    </section>
  );
};

export default LoginPage;
